{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Playlist Curator main window capabilities",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:default"
  ]
}
